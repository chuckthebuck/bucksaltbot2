name = "template_module"
repo = "https://github.com/chuckthebuck/bucksaltbot2"
entry_point = "modules.template_module.blueprint"
ui = true
redis_namespace = "template_module"
title = "Template Module"

# Optional: declare framework rights exposed by this module.
# Grant these with module-scoped atoms such as module:template_module:run_jobs.
# rights = ["manage", "run_jobs", "edit_config"]

# For cron-only modules, add:
# [[cron_jobs]]
# name = "some-job"
# run = "daily at 01:00"
# # Or use raw cron for advanced schedules:
# # schedule = "0 1 * * *"
# endpoint = "/api/v1/template_module/cron/some-job"
# timeout_seconds = 300
