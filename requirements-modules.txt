# Optional Chuck the Buckbot Framework modules.
#
# Use direct Git dependencies so modules can stay in their own repositories
# without publishing anything to PyPI or npm. Pin to a tag or commit for
# production deploys.
#
# The framework registers only names listed in enabled-modules.txt.
chuck-the-4awardhelper @ git+https://github.com/chuckthebuck/module4awardhelper@main
