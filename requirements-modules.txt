# Optional Chuck the Buckbot Framework modules.
#
# Modules are vendored into the framework deploy repo under vendor/modules
# as git subtree snapshots. Toolforge buildservice installs these local paths;
# it does not fetch module repos during deploy.
#
# The framework registers only names listed in enabled-modules.txt.
./vendor/modules/four_award
./vendor/modules/chuck_file_changer
